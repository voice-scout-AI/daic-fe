import React from 'react';
import Header from './Header';

interface StepLayoutProps {}

const steps = [{ label: 'Upload' }, { label: 'Transform' }, { label: 'Generate' }];

const StepLayout = { currentStep };
